import { defineConfig } from 'tsdown';

/**
 * Default tsdown configuration for libraries generated by Knexe.
 * Produces both ESM and CJS outputs with TypeScript declarations.
 */
export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm', 'cjs'],
  dts: true,
  clean: true,
});

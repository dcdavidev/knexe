import { defineConfig } from 'tsdown';

/**
 * tsdown build configuration for the library.
 * Generated by @knexe/js:lib
 */
export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm', 'cjs'],
  dts: true,
  clean: true,
  minify: false,
  sourcemap: true,
  outDir: 'dist',
  /**
   * Package name, injected from generator schema.
   */
  name: '@knexe/<%= name %>',
});

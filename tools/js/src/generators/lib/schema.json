{
  "$schema": "https://json-schema.org/schema",
  "$id": "Lib",
  "title": "Knexe JS/TS library generator",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "The path where the project should be created",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Where do you want to create the project? (e.g. devkit/datasource/psql)"
    },
    "name": {
      "type": "string",
      "description": "The Nx project name (used in package.json and nx.json)",
      "x-prompt": "What name would you like to use? (e.g. datasource-psql)"
    },
    "publishable": {
      "type": "boolean",
      "description": "Whether to allow publishing this project to the registry or not",
      "default": false,
      "x-prompt": "Do you want to publish this project?"
    }
  },
  "required": ["directory", "name"]
}
